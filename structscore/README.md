# StructScore

This directory contains the implementation of the proposed discourse-driven evaluation approach.

Recommended future components:

```text
structscore/
├── __init__.py
├── notebooks/      # migrated notebook experiments and scratch work
├── chunking/       # discourse-inspired segmentation
├── scoring/        # NLI or sentence-level scoring components
├── aggregation/    # discourse-aware score aggregation
└── utils/          # shared helpers
```

As you upload notebook cells, we can gradually convert them into reusable modules here.
