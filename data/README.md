# Data

This directory stores datasets and dataset-related artifacts used in the paper.

Suggested organization:

```text
data/
```

Add subfolders as needed based on the benchmarks and preprocessing pipeline.

Current benchmark artifact:

- `data/diversumm_test.csv`: cleaned-in-repo DiverSumm benchmark file with predicted MiniCheck segment features.
